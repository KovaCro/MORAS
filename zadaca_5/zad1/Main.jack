class Main {
  function void main() {
    var Array A;
    var int length, i;
    let length = Keyboard.readInt("How many numbers? ");
    let A = Array.new(length);
    let i = 0;
    while (i < length) {
      let A[i] = Keyboard.readInt("Enter number: ");
      let i = i + 1;
    }
    let A = Qsort.sort(A, length);
    let i = 0;
    while(i < length){
      do Output.printInt(A[i]);
      do Output.printString(" ");
      let i = i+1;
    }
    do Output.println();
    return;
  }
}
